---@module 'opts.utils'

---@class Opts.Utils
return {
  logger = require "opts.utils.logger",
  picker = require "opts.utils.picker",
  layout = require "opts.utils.layout",
  config = require "opts.utils.config",
}
